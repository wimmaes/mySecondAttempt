apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/wimmaes/mysecondattempt:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/wimmaes/mysecondattempt:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: mysecondattempt
        chartPath: charts/mysecondattempt
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/wimmaes/mysecondattempt"
          image.tag: "{{.DIGEST_HEX}}"
